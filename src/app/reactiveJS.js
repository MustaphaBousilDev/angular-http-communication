/** 
 * RxJS Operators
 * 
 * of <- debounceTime
 * from <- distinct until changed 
 * map <- take,takeUntil , takeWhile
 * tap <- takeLast 
 * share <- mergeMap 
 * switchMap <- flatMap 
 * concat <- forkJoin
*/